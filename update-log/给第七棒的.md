# 给第七棒的.md —— 第六棒 → 第七棒 交接文档

> 生成日期：2026-07-29
> 上游交付：第五棒（见 `给第六棒的.md`、`第六棒update.md`）

---

## 一、第六棒工作总览

第六棒的核心任务：**地图地形纹理系统重构 + 全套瓦片贴图集成**。今天的工作偏"视觉层"而非玩法层 — 将美术绘制的 16 张地形瓦片 PNG 接入游戏引擎，并搭建了一套灵活的多层纹理解析管线。

### 1. 地形纹理渲染管线（MapRenderer 重构）

- **文件**：`src/rendering/MapRenderer.js`
- **配套改动**：`src/main.js`（`await mapRenderer.init()`）

**改动要点**：

| 组件 | 说明 |
|------|------|
| 预加载 | `init()` 用原生 `Image` 预加载所有地形纹理 → `PIXI.Texture` 缓存，首次绘制即就绪 |
| 渲染层级 | 纹理 Sprite（顶层）→ 纯色 Graphics（底层，含网格线），纹理未就绪也不黑屏 |
| 拖拽/缩放 | 自动触发 `_drawTerrainChunk()` 全量重绘 |
| 纹理解析链 | 四级优先级：`neighborOverrides → rowPatterns → variants → proximityOverrides → 默认` |

### 2. 银弹级纹理解析链

这个是今天最大的工程价值：**所有纹理选择逻辑都配置驱动，零硬编码**。加新地形/新素材只需改 JSON。

```
neighborOverrides  → "如果邻居是 X，用纹理 Y"（边界过渡）
rowPatterns        → "每 N 行用纹理 Z"（周期性纹理）
variants           → "X% 概率随机换变体纹理"（散落点缀，含防相邻算法）
proximityOverrides → "靠近坐标 P 半径 R 内替换纹理"（POI 标记，如矿洞入口）
默认纹理            → groundType.texture
```

### 3. 黑屏修复

- **根因**：`MapRenderer._centerView()` 查 `initialCamera`，`base_map.json` 只有 `viewportCenter` → 相机偏到 (100,100) → 火把不在视野 → 迷雾全黑
- **修复**：`base_map.json` 加 `initialCamera: { gridX: 80, gridY: 113 }`
- 还创建了 19 个静音占位 `.wav` 文件，消除启动时的音效 404 错误

### 4. 地形纹理完整配置

| 类型 | 主纹理 | 变体 | 边界过渡 |
|------|--------|------|---------|
| G 草地 | `tile_grass.png` | `daisy.png` (5%), `forsythia.png` (3%) | ↓水→`grasswater`, ↑水→`watergrass`, ↑土→`landgrass` |
| W 水源 | `water.png` | — | — |
| D 土地 | `land.png` | `land1.png` (2%) | — |
| F 林地 | `forest.png` | `forest1.png` (2%) | ↑草/水/土→`grassforest`, ↓草/水/土→`forest1` |
| R 石头 | `rock1.png` | `rock.png` (3%) | ↓草/水/土/林→`stone.png`, 矿洞近邻→`stone1.png` |
| M 山脉 | 纯色回退 | — | — |
| B 屏障 | 纯色回退 | — | — |

### 5. 新增的配置引擎能力

**`config/maps/base_map.json`** 中每种 `groundType` 新增可选字段：

```jsonc
{
  "texture": "主纹理路径",
  "variants": [{ "texture": "变体路径", "chance": 0.05 }],      // 多数组支持
  "neighborOverrides": [{ "dx": 0, "dy": 1, "match": "G", "texture": "..." }],
  "rowPatterns": [{ "interval": 3, "fromBottom": true, "texture": "..." }],
  "proximityOverrides": [{ "center": {"gridX": 79, "gridY": 107}, "radius": 0, "match": "assets/map/stone.png", "texture": "..." }]
}
```

---

## 二、仍存在的已知问题

### 从第五棒遗留

1. **道路刷新 Bug**：刷新页面后道路可能消失，立即同处放置失败并吞材料
2. **建筑进度条闪烁**：时间 +1 tick 时完成度瞬间 +1 然后复原（视觉瑕疵）
3. **敌人生成位置不明**：能刷新但不知道在哪
4. **炼金+人文修饰器叠加**：乘法叠算时的数值膨胀风险

### 第六棒可能引入的

5. **拖拽性能**：每次拖拽都重建全部 Sprite，大地图（200×200）下可能掉帧，建议后续做 Sprite 池复用
6. **纹理未找到**：`_getTexture()` 找不到纹理时静默跳过，不报错，排查困难时可临时在跳过处加 `console.warn`
7. **proximityOverrides 的 radius=0**：矿洞入口的 `stone1.png` 仅在精确坐标 (79,107) 生效，若该格不是 R 型则无效果。后续建议改为 `radius: 1` 或去掉 `match` 过滤器

---

## 三、后续可扩展方向

| 方向 | 当前状态 | 建议突破口 |
|------|---------|-----------|
| **山脉/屏障纹理** | 纯色回退 | 加 `texture` 字段即可，引擎侧无需改动 |
| **更多变体/过渡纹理** | G/W/D/F/R 已覆盖 | 加 `variants`/`neighborOverrides` 条目，纯配置 |
| **Sprite 池复用** | 每次全量重建 | 可见范围 + 复用池 → 大幅提帧率 |
| **配置 DRY** | `neighborOverrides` 大量重复 | 引擎侧支持 `match: ["G","W","D","F"]` 数组语法 |
| **proximityOverrides 扩展** | 只有矿洞入口 | 用于山麓/森林/回收场入口特殊标记 |
| **水道边缘过渡** | 水域无 neighborOverrides | 可仿照草地/林地加水岸过渡纹理 |
| **第五棒遗留 Bug** | 道路/进度条/敌人生成 | RoadSystem / CombatSystem 排查 |

---

## 四、关键系统依赖（更新后）

```
MapRenderer._drawTerrainChunk()
  ├── _preloadTerrainTextures()     ← init() 时调用
  ├── _isVariantCandidate(x,y,p)    ← 哈希 + 乘法混合
  ├── _getTexture(path)             ← Map lookup
  └── groundTypes[char].texture/variants/neighborOverrides/proximityOverrides
```

渲染数据流不变：
```
main.js → MapRenderer.init() → 预加载纹理 → GameLoop → _drawTerrainChunk() → Sprite + Graphics
```

---

## 五、快速参考

| 任务 | 位置 |
|------|------|
| 加新地形纹理 | `config/maps/base_map.json` → 对应 `groundType` 加 `texture` 字段 |
| 加新变体纹理 | 同上，加 `variants` 数组条目 |
| 加新边界过渡 | 同上，加 `neighborOverrides` 条目 |
| 加新 POI 标记 | 同上，加 `proximityOverrides` 条目 |
| 纹理预加载逻辑 | `MapRenderer._preloadTerrainTextures()` |
| 纹理绘制逻辑 | `MapRenderer._drawTerrainChunk()` |
| 变体选择算法 | `MapRenderer._isVariantCandidate()` + 两遍扫描 |
| 素材更新后 | `node scripts/generate-asset-manifest.js` |

---

## 六、运行环境

```bash
node scripts/generate-asset-manifest.js   # 素材变动后执行
npx http-server -p 8080 -c-1 --cors       # 启动服务
# 浏览器打开 http://127.0.0.1:8080
```

- 平台：Windows 11 / PowerShell
- 架构铁律见 `CLAUDE.md` 第四章、`AGENTS.md`
- 第六棒详细记录见 `第六棒update.md`
- 纹理解析链完整说明见 `src/rendering/MapRenderer.js` 中 `_drawTerrainChunk()` 方法

---

> 交接文档结束。祝第七棒开发顺利！
> 如需更多上下文，请务必阅读 `CLAUDE.md`、`AGENT.md`、`AGENTS.md`、`docs/` 下各设计文档，以及本文件提到的各棒交接文档。
