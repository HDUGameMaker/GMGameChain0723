# 给第九棒的.md —— 第八棒 → 第九棒 交接文档

> 生成日期：2026-07-31
> 上游交付：第七棒（见 `给第八棒的.md`）

---

## 一、第八棒工作总览

第八棒主要做了以下工作：

1. 修改相邻加成，简化算法和设计。
2. 修复编辑器内容缺失部分。
3. 新增战斗系统，现在需要训练部队应对入侵。
4. 新增兵种、阵型、入侵相关配置。
5. 重做人文政策系统。
6. 修改了部分建筑数值。

---

## 二、给第九棒的重点提示

- 战斗与入侵链路已经进入玩法主干，需要优先确认训练、兵种、阵型、入侵触发、UI 面板和存档之间的数据闭环。
- 人文政策系统经过重做，后续改动前先读 `docs/人文树设计文档.md`、`config/culture.json`、`config/doctrines.json` 和 `src/systems/CultureSystem.js`。
- 相邻加成算法已被简化，若出现建筑产出异常，优先检查 `config/adjacency-bonuses.json` 与 `BuildingSystem` 的加成计算入口。
- 编辑器内容缺失已修复过，继续修改 `planner/`、`editor/`、`sound-editor/` 时注意保持脚本加载顺序。

---

## 三、当前整理约定

- 棒次交接文档统一放在 `update-log/`，文件名统一为 `给第X棒的.md`。
- 零散 update 内容已合并进对应下一棒交接文档，不再单独保留在 `update-log/`。
- AI 协作说明统一放在 `.agent/`，合并入口为 `.agent/AI_ASSISTANT_GUIDE.md`。
- 游戏运行日志统一放在 `log/`。

---

## 四、运行环境

```bash
node scripts/generate-asset-manifest.js
npx http-server -p 8080 -c-1 --cors
# http://127.0.0.1:8080
```

必须使用 HTTP 服务运行，不能直接用 `file://`。
